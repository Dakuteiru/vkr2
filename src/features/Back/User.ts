export type UserProfileToken =
    {
        email: string;
        token: string;
};
export type UserProfile = {
    userName: string;
    email: string;
}
export type UserList = {
    id: string;
    userName: string;
    email: string;
}